<Window x:Class="BudgetManagement.Views.Dialogs.SimpleInputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Input" 
        Height="200" Width="400"
        MinHeight="180" MinWidth="350"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        FontFamily="Segoe UI"
        FontSize="16"
        Background="{DynamicResource BackgroundBrush}"
        ShowInTaskbar="False">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Views/Styles/ModernDesign.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Message -->
        <TextBlock x:Name="MessageTextBlock" Grid.Row="0" TextWrapping="Wrap" 
                   FontSize="14" Margin="0,0,0,15" Foreground="{DynamicResource PrimaryTextBrush}"/>

        <!-- Input Box -->
        <TextBox x:Name="InputTextBox" Grid.Row="1" 
                 Style="{StaticResource ModernTextBoxStyle}"
                 Height="44" Margin="0,0,0,10"/>

        <!-- Spacer -->
        <Border Grid.Row="2"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Cancel" Click="CancelButton_Click" 
                    Style="{StaticResource ModernSecondaryButtonStyle}" 
                    Width="80" Margin="0,0,10,0"/>
            <Button Content="OK" Click="OKButton_Click" 
                    Style="{StaticResource ModernSuccessButtonStyle}"
                    Width="80" IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>