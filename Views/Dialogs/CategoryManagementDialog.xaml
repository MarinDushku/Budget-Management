<Window x:Class="BudgetManagement.Views.Dialogs.CategoryManagementDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:BudgetManagement.Views.Dialogs"
        Title="Manage Spending Categories" 
        Height="600" Width="800"
        MinHeight="500" MinWidth="700"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        FontFamily="Segoe UI"
        FontSize="16"
        Background="#F8F9FA"
        ShowInTaskbar="False">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Views/Styles/ModernDesign.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            
        <Style x:Key="HeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="22"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="Margin" Value="0,0,0,20"/>
        </Style>
        
        <Style x:Key="SectionHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#444444"/>
            <Setter Property="Margin" Value="0,15,0,10"/>
        </Style>
        
        <Style x:Key="CategoryCardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="6"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#E0E0E0" Direction="270" ShadowDepth="2" BlurRadius="4" Opacity="0.3"/>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Margin" Value="4"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="MinWidth" Value="80"/>
        </Style>
        
        <Style x:Key="PrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource ActionButtonStyle}">
            <Setter Property="Background" Value="#2196F3"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#1976D2"/>
        </Style>
        
        <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource ActionButtonStyle}">
            <Setter Property="Background" Value="#F5F5F5"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
        </Style>
        
        <Style x:Key="DangerButtonStyle" TargetType="Button" BasedOn="{StaticResource ActionButtonStyle}">
            <Setter Property="Background" Value="#F44336"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#D32F2F"/>
        </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>  <!-- Header -->
            <RowDefinition Height="Auto"/>  <!-- Add New Section -->
            <RowDefinition Height="*"/>     <!-- Categories List -->
            <RowDefinition Height="Auto"/>  <!-- Action Buttons -->
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="White" BorderBrush="#DDD" BorderThickness="1" 
                CornerRadius="6" Padding="20" Margin="0,0,0,25">
            <StackPanel>
                <TextBlock Text="🏷️ Manage Spending Categories" Style="{StaticResource HeaderStyle}" 
                           HorizontalAlignment="Center"/>
                <TextBlock Text="Add, edit, or remove categories to organize your spending" 
                           FontSize="14" Foreground="#666" HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- Add New Category Section -->
        <Border Grid.Row="1" Style="{StaticResource CategoryCardStyle}" Margin="0,0,0,20">
            <StackPanel>
                <TextBlock Text="➕ Add New Category" Style="{StaticResource SectionHeaderStyle}"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBox x:Name="NewCategoryNameTextBox" Grid.Column="0" 
                             FontSize="16" Padding="12,10" Margin="0,0,15,0" 
                             MinHeight="45" MaxLength="50"
                             ToolTip="Enter category name (e.g., 'Entertainment', 'Utilities')"
                             KeyDown="NewCategoryNameTextBox_KeyDown"/>
                             
                    <Button x:Name="AddCategoryButton" Grid.Column="1" Content="Add Category" 
                            Style="{StaticResource PrimaryButtonStyle}" MinWidth="120"
                            Click="AddCategoryButton_Click"/>
                </Grid>
            </StackPanel>
        </Border>

        <!-- Categories List -->
        <Border Grid.Row="2" Style="{StaticResource CategoryCardStyle}">
            <StackPanel>
                <TextBlock Text="📋 Current Categories" Style="{StaticResource SectionHeaderStyle}"/>
                <TextBlock Text="Categories are shown in the order they appear in dialogs" 
                           FontSize="12" Foreground="#666" Margin="0,0,0,15"/>
                
                <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="300">
                    <ListBox x:Name="CategoriesListBox" BorderThickness="0" Background="Transparent"
                             ItemsSource="{Binding Categories}"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#F9F9F9" BorderBrush="#E0E0E0" BorderThickness="1" 
                                        CornerRadius="4" Padding="15" Margin="0,0,0,6">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>  <!-- Order -->
                                            <ColumnDefinition Width="*"/>     <!-- Name -->
                                            <ColumnDefinition Width="Auto"/>  <!-- Actions -->
                                        </Grid.ColumnDefinitions>
                                        
                                        <!-- Display Order -->
                                        <Border Grid.Column="0" Background="#2196F3" CornerRadius="12" 
                                                Width="24" Height="24" Margin="0,0,15,0">
                                            <TextBlock Text="{Binding DisplayOrder}" Foreground="White" 
                                                       FontSize="12" FontWeight="Bold" 
                                                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        
                                        <!-- Category Name -->
                                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding Name}" FontSize="16" FontWeight="Medium"
                                                       Foreground="#333333"/>
                                            <TextBlock Text="{Binding IsActive, Converter={x:Static local:BooleanToStatusConverter.Instance}}" 
                                                       FontSize="12" Foreground="#666666"/>
                                        </StackPanel>
                                        
                                        <!-- Action Buttons -->
                                        <StackPanel Grid.Column="2" Orientation="Horizontal">
                                            <Button Content="↑" Style="{StaticResource SecondaryButtonStyle}" 
                                                    ToolTip="Move Up" Width="35" Height="35"
                                                    Click="MoveUpButton_Click" CommandParameter="{Binding}"/>
                                            <Button Content="↓" Style="{StaticResource SecondaryButtonStyle}" 
                                                    ToolTip="Move Down" Width="35" Height="35"
                                                    Click="MoveDownButton_Click" CommandParameter="{Binding}"/>
                                            <Button Content="✏️" Style="{StaticResource PrimaryButtonStyle}" 
                                                    ToolTip="Edit" Width="35" Height="35"
                                                    Click="EditButton_Click" CommandParameter="{Binding}"/>
                                            <Button Content="🗑️" Style="{StaticResource DangerButtonStyle}" 
                                                    ToolTip="Delete" Width="35" Height="35"
                                                    Click="DeleteButton_Click" CommandParameter="{Binding}"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </ScrollViewer>
            </StackPanel>
        </Border>

        <!-- Dialog Action Buttons -->
        <Border Grid.Row="3" Background="#F5F5F5" BorderBrush="#DDD" BorderThickness="1" 
                CornerRadius="6" Padding="20" Margin="0,20,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="🔄 Reset to Default" Style="{StaticResource SecondaryButtonStyle}" 
                        Click="ResetToDefaultButton_Click" MinWidth="150" Margin="0,0,15,0"
                        ToolTip="Restore default categories (Family, Personal, Marini)"/>
                <Button Content="❌ Cancel" Style="{StaticResource SecondaryButtonStyle}" 
                        Click="CancelButton_Click" MinWidth="100" Margin="0,0,15,0"/>
                <Button Content="✅ Save Changes" Style="{StaticResource PrimaryButtonStyle}" 
                        Click="SaveButton_Click" MinWidth="130"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>